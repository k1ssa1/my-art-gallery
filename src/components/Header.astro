---
import { Image } from "astro:assets";

import photo1 from "../../public/photo_one.webp";
import photo2 from "../../public/photo_two.webp";
import photo3 from "../../public/photo_three.webp";
---

<div class="header">
  <div class="image_displayer">
    <Image src={photo1} alt="two models in black dresses" class="images" />
    <Image src={photo2} alt="runway show" class="images" />
    <Image src={photo3} alt="two modelss wearing dresses" class="images" />
  </div>
  <div class="header_content">
    <h1 class="header_title">
      Explore The Atelier Gallery: Where Fashion Meets Art
    </h1>
    <p class="header_text">
      Discover exclusive fashion-inspired artworks from emerging and established
      designers. Curated exhibitions, limited-edition pieces, and a unique art
      experience await you.
    </p>
    <a href="" class="header_cta_link">Learn more</a>
  </div>
</div>

<script>
  import anime from "animejs";

  document.addEventListener("DOMContentLoaded", () => {
    anime({
      targets: ".images",
      opacity: [0, 1],
      duration: 4000,
      easing: "easeOutExpo",
    });
    anime({
      targets: ".images",
      opacity: [0, 1],
      duration: 4000,
      easing: "easeOutExpo",
    });
    anime({
      targets: ".header_title",
      opacity: [0, 1],
      duration: 4000,
      easing: "easeOutExpo",
    });
    anime({
      targets: ".header_text",
      opacity: [0, 1],
      duration: 4000,
      easing: "easeOutExpo",
    });
    anime({
      targets: ".header_cta_link",
      opacity: [0, 1],
      duration: 4000,
      easing: "easeOutExpo",
    });
  });
</script>

<style>
  .header {
    margin: 2rem 1rem;
    padding: 1rem;
    display: flex;
    flex-direction: row;
  }

  .image_displayer {
    width: 80%;
    display: flex;
    flex-direction: row;
    gap: 2px;
  }

  .images {
    width: 300px;
    height: 100%;
  }

  .header_content {
    display: flex;
    flex-direction: column;
    padding: 0 2rem;
  }

  .header_title {
    font-family: "Karla", Arial, Helvetica, sans-serif;
    text-transform: uppercase;
    font-size: 2rem;
    font-weight: 900;
    color: #010101;
  }

  .header_text {
    font-family: "Karla", Arial, Helvetica, sans-serif;
    font-size: 1.5rem;
    font-weight: 400;
    line-height: 28px;
    color: #707070;
    margin: 1rem 0;
  }

  a {
    text-decoration: none;
    font-size: 1.5rem;
    font-family: "Karla", Arial, Helvetica, sans-serif;
    font-weight: 300;
    color: #010101;
    width: fit-content;
    margin: 1rem 0;
  }

  a:hover {
    font-weight: 800;
  }
</style>
